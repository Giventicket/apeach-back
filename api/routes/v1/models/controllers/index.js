const uploadModel = require('./uploadModel');
const parseForm = require('./parseForm');
const getModel = require('./getModel');
const getModels = require('./getModels');

module.exports = { uploadModel, parseForm, getModel, getModels };
